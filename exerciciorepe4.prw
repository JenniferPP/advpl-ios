/* Escreva um algoritmo para imprimir os números de 1 (inclusive) a 10 (inclusive)
em ordem crescente.*/

#include "protheus.ch"

user function umadez ()

local ncrescimento := 1

for ncrescimento:=1 to 10 
 ConOut( ncrescimento )

next ncrescimento

return